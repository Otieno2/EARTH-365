{
  "name": "@earth365/shared-types",
  "version": "0.0.1",
  "private": true,
  "description": "Types shared between the EARTH-365 backend and TypeScript clients. The Unity client receives these via a generated OpenAPI client.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint configured for shared-types'"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
